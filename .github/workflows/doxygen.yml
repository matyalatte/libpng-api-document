name: Deploy docs

on:
  push:
    branches:
      - main
    paths:
      - 'libpng-api-document.h'
      - 'docs/**'
      - 'Doxyfile'
      - 'DoxygenLayout.xml'
      - '.github/workflows/doxygen.yml'
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install doxygen 1.9.8
      env:
        DOXYGEN_VERSION: 1.9.8
      run: |
        wget https://github.com/doxygen/doxygen/releases/download/Release_${DOXYGEN_VERSION//./_}/doxygen-${DOXYGEN_VERSION}.linux.bin.tar.gz
        tar xzvf doxygen-${DOXYGEN_VERSION}.linux.bin.tar.gz
        cd doxygen-${DOXYGEN_VERSION}
        sudo make install

    - name: Run Doxygen
      run: |
        doxygen ./Doxyfile

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: ./docs/html

  deploy:
    environment:
      name: github-pages
      url: matyalatte.github.io/libpng-api-document
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
